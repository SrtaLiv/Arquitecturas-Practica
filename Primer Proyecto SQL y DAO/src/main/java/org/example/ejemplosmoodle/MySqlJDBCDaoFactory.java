package org.example.ejemplosmoodle;

public class MySqlJDBCDaoFactory {
    /*
    // Inicializar y conectarse a la base de datos

    String driver = "com.mysql.cj.jdbc.Driver";

    //conexion a la base de datos
    String uri = "jdbc:mysql://localhost:3307/arquidb";

    //Creamos conexion
    {
        try {
            Connection con = DriverManager.getConnection(uri, "root", "root");
            con.setAutoCommit(false);

            // createTables(con); //Metodo para crear tablas dah
            //addPerson(con, 1, "Olivia", 19);
            //addPerson(con, 2, "Santiago", 23);
        } catch (
                SQLException e) {
            throw new RuntimeException(e);
        }
    }

    @Override
    public List<PersonaDTO> listAllPersonas() {
        //Connection = DriverManager ...
        //PreparedStatement ps =
        //connection.prepareStatement("select * from ...");
        //ResultSet = ps.executeQuery()
        // and so on...

        return null;
    }
*/
}
